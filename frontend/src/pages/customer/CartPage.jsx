import './CartPage.css';

/** C-01 — not built yet; the route and the layout around it are. */
export default function CartPage() {
  return (
    <section className="cart-page">
      <h1 className="cart-page__title">Cart</h1>
      <p className="cart-page__todo">C-01 is not implemented yet.</p>
    </section>
  );
}
