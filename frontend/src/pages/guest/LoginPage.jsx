import './LoginPage.css';

/** G-09 — not built yet; the route and the layout around it are. */
export default function LoginPage() {
  return (
    <section className="login-page">
      <h1 className="login-page__title">Sign in</h1>
      <p className="login-page__todo">G-09 is not implemented yet.</p>
    </section>
  );
}
