/**
 * Admin screens. The Admin branch owns `pages/admin/` (assignment §6), so its routes land here as
 * those pages arrive. Shape to copy — see customer.routes.jsx for a live example:
 *
 *   { element: <RequireAuth />, children: [
 *       { element: <RequireRole allow={['ADMIN']} />, children: [
 *           { element: <AdminLayout />, children: [{ path: '/admin', element: … }] }] }] }
 */
export const adminRoutes = [];
