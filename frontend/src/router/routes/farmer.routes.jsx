/**
 * Farmer screens. The Farmer branch owns `pages/farmer/` (assignment §6), so its routes land here as
 * those pages arrive. Shape to copy — see customer.routes.jsx for a live example:
 *
 *   { element: <RequireAuth />, children: [
 *       { element: <RequireRole allow={['FARMER']} />, children: [
 *           { element: <FarmerLayout />, children: [{ path: '/farmer', element: … }] }] }] }
 */
export const farmerRoutes = [];
