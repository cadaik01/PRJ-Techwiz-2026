# Point at Django (Vite proxies /api → http://localhost:8000).
VITE_API_URL=/api
VITE_WS_BASE_URL=/ws

# true = MSW mocks (local FE without full BE routes). false = live Django API.
VITE_USE_MOCK=true
