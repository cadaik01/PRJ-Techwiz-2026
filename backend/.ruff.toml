# Lint settings shared by local runs and CI (.github/workflows/backend-tests.yml).
# Run with: ruff check .
line-length = 120

# Generated by makemigrations: not ours to format.
extend-exclude = ["*/migrations/*"]

[lint]
# E/W pycodestyle, F pyflakes, I import order.
select = ["E", "F", "W", "I"]
